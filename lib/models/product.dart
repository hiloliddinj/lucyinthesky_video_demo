class Product {
  final String image, price, name, regularVideo, regularVideoPoster, special;
  Product(this.image, this.price, this.name, this.regularVideo,
      this.regularVideoPoster, this.special);
}
